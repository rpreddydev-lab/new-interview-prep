================================================================
gRPC AND PROTOCOL BUFFERS -- POINTER
================================================================

This topic is fully covered in:

  10_Backend/10_gRPC/

That module includes:
  - gRPC vs REST vs GraphQL comparison
  - Protocol Buffers (protobuf) schema design
  - Unary, server-streaming, client-streaming, bidirectional streaming
  - Spring Boot gRPC server and client setup
  - Interceptors, error handling, and deadlines
  - gRPC in microservices: service-to-service communication
  - Interview Q&A

For system design context (when to choose gRPC), see:
  08_System_Design/03_Networking_and_Communication/01_REST_vs_GraphQL_vs_gRPC.txt
